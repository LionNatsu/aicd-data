name: Notify AICD

on:
  push:
    branches: [main]

jobs:
  notify:
    runs-on: ubuntu-latest
    steps:
      - name: Trigger AICD rebuild
        env:
          GH_TOKEN: ${{ secrets.PAT_AICD_DISPATCH }}
        run: |
          gh api repos/LionNatsu/aicd/dispatches \
            -f event_type=aicd-data-updated
